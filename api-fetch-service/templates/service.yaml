apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-fetch-service.fullname" . }}
spec:
  selector:
    app: {{ include "api-fetch-service.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
