apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-fetch-service
spec:
  rules:
    - host: api.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-fetch-service
                port:
                  number: 80